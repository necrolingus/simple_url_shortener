- Use express rate limit. Make sure the MAX_requests and Requests_Time_Window are configurable via environment variables. Replace the currently rate limiting with express rate limit.
- On the post login screen, a list of all the short URLs must be displayed with the following information:
-- The short URL, the long URL, the date is was created, the expiry date, and count of the number of clicks on this short URL. Each short URL must have a delete button next to it so it can be deleted.

- Make sure this list of URLs fit neatly on the screen. Cut off the Long URL if needed and shorten the datetime values.

- Playwright MCP is installed so use it.
- Create your own skills to help you do your job.
- This project is hooked up to a working database.
- Check the directives in the directives folder.